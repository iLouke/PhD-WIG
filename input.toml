title = "WIG Multi-Vehicle Simulation"
dt = 0.05

# Switch to vehicle_demo.toml for the geometry-init pipeline.
# Replace with vehicle_alpha.toml / vehicle_beta.toml once the real .dat files are ready.
vehicles = ["vehicle_demo.toml"]